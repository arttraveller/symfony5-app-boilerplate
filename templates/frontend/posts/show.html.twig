{% extends 'frontend/base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}

    <h2>{{ post.title }}</h2>

    <div class="post-content">
        {{ post.text|purify }}
    </div>

{% endblock %}
